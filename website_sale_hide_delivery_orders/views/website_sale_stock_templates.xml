<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template
        id="orders_followup_shipping"
        name="Delivery Orders Groups"
        inherit_id="sale_stock.sale_order_portal_content_inherit_sale_stock"
    >
        <div t-if="delivery_orders" position="attributes">
            <attribute name="groups">sales_team.group_sale_manager</attribute>
        </div>

        <div t-if="returns" position="attributes">
            <attribute name="groups">sales_team.group_sale_manager</attribute>
        </div>
    </template>
</odoo>
