<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template
        id="address"
        inherit_id="website_sale.address"
        name="Split name field"
        customize_show="True"
        active="True"
    >

        <xpath expr="(//input[@name='name']/..)" position="attributes">
            <attribute name="style">display: none;</attribute>
        </xpath>
        <xpath expr="(//input[@name='name'])" position="attributes">
            <attribute name="disabled">disabled</attribute>
        </xpath>

        <xpath expr="(//input[@name='name']/..)" position="after">
            <div
                t-attf-class="form-group #{error.get('firstname') and 'o_has_error' or ''} col-lg-6 div_firstname"
                id="div_firstname"
            >
                <label class="col-form-label" for="firstname">Firstname</label>
                <input
                    type="text"
                    name="firstname"
                    required="1"
                    t-attf-class="form-control #{error.get('firstname') and 'is-invalid' or ''}"
                    t-att-value="'firstname' in checkout and checkout['firstname']"
                />
            </div>
            <div
                t-attf-class="form-group #{error.get('lastname') and 'o_has_error' or ''} col-lg-6 div_lastname"
                id="div_lastname"
            >
                <label class="col-form-label" for="lastname">Lastname</label>
                <input
                    type="text"
                    name="lastname"
                    t-attf-class="form-control #{error.get('lastname') and 'is-invalid' or ''}"
                    t-att-value="'lastname' in checkout and checkout['lastname']"
                />
            </div>
        </xpath>
    </template>
</odoo>
